export default function Admin() {
  return (
  // If the account is verified, have redirect to the admin dashboard
  <div className = "w-full h-screen z-100 bg-white">
    <div className = "grid grid-cols-12 w-full">
        <div className = "col-start-3 mt-20 text-black">
            Admin verify page
        </div>
    </div>
  </div>
  );
}
